import Box from "@mui/material/Box";

export const Palette = () => {
  return (
    <Box sx={{ height: 100, display: "flex", alignItems: "center", backgroundColor: "red" }}>
      palette
    </Box>
  )
}
